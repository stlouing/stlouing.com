---
interface Props {
  title?: string
  class?: string
}

const { title, class: className, ...rest } = Astro.props
---

<div class:list={[className]} {...rest}>
  {title && <h1>{title}</h1>}
  <ul class="list">
    <slot />
  </ul>
</div>
