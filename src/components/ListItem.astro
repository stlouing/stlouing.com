---
interface Props {
  title: string
  href?: string
  meta?: string
  description?: string
  leader?: boolean
}

const { title, href: linkHref, meta, description, leader = false } = Astro.props
const Tag = linkHref ? 'a' : 'div'
const hasIcon = Astro.slots.has('icon')
---

<li>
  <Tag class:list={['list-item', { 'has-icon': hasIcon, 'hover-wash': Boolean(linkHref) }]} href={linkHref}>
    {
      hasIcon && (
        <span class="list-icon" aria-hidden="true">
          <slot name="icon" />
        </span>
      )
    }
    <span class="list-item-body">
      <div class="list-row">
        <span class="list-title">{title}</span>
        {leader && <span class="list-leader" aria-hidden="true"></span>}
        {meta && <span class="list-meta">{meta}</span>}
        <slot name="meta" />
      </div>
      {description && <p class="list-description">{description}</p>}
      <slot />
    </span>
  </Tag>
</li>

<style>
  .list-item {
    display: block;
    padding-block: var(--spacing-sm);
    padding-inline: var(--spacing-sm);
    margin-inline: calc(-1 * var(--spacing-sm));
    border-radius: var(--radius);
    color: inherit;
    text-decoration: none;
  }

  .list-item.has-icon {
    display: flex;
    align-items: flex-start;
    gap: var(--spacing-sm);
  }

  .list-item-body {
    flex: 1;
    min-width: 0;
  }

  .list-icon {
    flex-shrink: 0;
    display: inline-flex;
  }

  .list-icon :global(.icon) {
    width: 1rem;
    height: 1rem;
    margin-block-start: 2px;
    color: var(--list-icon-color, var(--color-accent));
  }

  a.list-item:hover .list-title {
    color: var(--list-hover-color, var(--color-accent));
    text-decoration: underline;
    text-decoration-thickness: 2px;
    text-underline-offset: 2px;
  }

  .list-row {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: var(--spacing-sm);
    margin-block: 0 2px;
  }

  .list-leader {
    flex: 1;
    min-width: var(--spacing-md);
    border-block-end: 2px dotted var(--color-line-input);
  }
</style>
