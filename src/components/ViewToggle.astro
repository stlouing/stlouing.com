<div class="field">
  <label for="view-toggle">View</label>
  <div id="view-toggle" class="view-toggle" data-view-toggle hidden>
    <button type="button" data-view-set="map" aria-pressed="true">Map</button>
    <button type="button" data-view-set="list" aria-pressed="false">List</button>
  </div>
</div>

<style>
  .view-toggle {
    display: inline-flex;
    border: 1px solid var(--color-line-input);
    border-radius: var(--radius);
    font-family: var(--font-mono);
    font-size: var(--font-size-xs);
    padding: 4px;
    height: 31px;
    gap: var(--spacing-xs);

    label {
      display: block;
    }

    button {
      border-radius: 2px;
      border: 0;
      padding: var(--spacing-xs) var(--spacing-sm);
      background: var(--color-surface);
      color: var(--color-text-muted);
      cursor: pointer;
      height: unset;
      line-height: 1;

      &:hover {
        color: var(--color-text);
      }

      &[aria-pressed='true'] {
        font-weight: 600;
        background: var(--color-accent);
        color: var(--color-background);
        outline: none;
        border: 1px solid var(--color-accent);
        box-shadow: 0px 0px 0px 1px var(--color-accent);
      }

      &:focus-visible {
        outline: 2px solid var(--color-accent);
        outline-offset: 2px;
      }
    }
  }
</style>
