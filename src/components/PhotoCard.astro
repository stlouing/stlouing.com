---
// One food card — photo, serif title, tagline, writeup teaser — the whole card
// is a single link. Shared by the homepage Food section (vertical, photo on
// top) and /best/ (horizontal: text 2/3, photo 1/3, via the `horizontal` prop).
// Cards sit in a `.photo-cards` grid (global.css); each page's own margins stay
// page-side. The text wrapper is display:contents in the vertical layout, so
// the homepage markup renders exactly as it did before the wrapper existed.
import type { CollectionEntry } from 'astro:content'
import { Image } from 'astro:assets'
import { href } from '../lib/url'

interface Props {
  place: CollectionEntry<'food'>
  teaser?: string
  // Optional mono kicker above the title (e.g. the neighborhood on /best/).
  eyebrow?: string
  // Text 2/3 + photo 1/3 side by side (stacks back to vertical on phones).
  horizontal?: boolean
}

const { place, teaser, eyebrow, horizontal = false } = Astro.props
---

<article class="photo-card">
  <a
    class:list={['photo-card-link', 'hover-wash', { 'is-horizontal': horizontal }]}
    href={href(`/food/${place.id}/`)}>
    {
      place.data.photo && (
        <Image class="photo-card-media" src={place.data.photo} alt={place.data.title} width={520} />
      )
    }
    <span class="photo-card-text">
      {eyebrow && <span class="photo-card-eyebrow eyebrow eyebrow--muted">{eyebrow}</span>}
      <span class="photo-card-title title-serif title-link">{place.data.title}</span>
      {
        place.data.description && (
          <span class="photo-card-description desc-muted">{place.data.description}</span>
        )
      }
      {teaser && <span class="photo-card-excerpt">{teaser}</span>}
    </span>
  </a>
</article>

<style>
  .photo-card {
    margin: 0;
    min-width: 0;
  }

  .photo-card-link {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xs);
    height: 100%;
    padding: var(--spacing-sm);
    margin: calc(-1 * var(--spacing-sm));
    border-radius: var(--radius);
    color: var(--color-text);
    text-decoration: none;
  }

  /* Vertical (default): the wrapper is invisible to layout. */
  .photo-card-text {
    display: contents;
  }

  .photo-card-link :global(.photo-card-media) {
    width: 100%;
    height: var(--card-photo-height);
    object-fit: cover;
    margin-block-end: var(--spacing-xs);
  }

  .photo-card-description {
    font-size: var(--font-size-md);
    line-height: 1.45;
    margin-bottom: var(--spacing-xs);
    display: -webkit-box;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
    overflow: hidden;
    flex-shrink: 0;
    font-style: italic;
  }

  .photo-card-excerpt {
    font-family: var(--font-serif);
    font-size: var(--font-size-md);
    line-height: 1.45;
    color: var(--color-text);
    display: -webkit-box;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 3;
    overflow: hidden;
    flex-shrink: 0;
  }

  /* Horizontal: text column 2/3, photo 1/3 on the right, photo cropped to the
     text column's height. Falls back to the vertical stack on phones. */
  @media (min-width: 601px) {
    .photo-card-link.is-horizontal {
      flex-direction: row;
      gap: var(--spacing-md);
    }

    .photo-card-link.is-horizontal .photo-card-text {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-xs);
      flex: 2;
      min-width: 0;
    }

    .photo-card-link.is-horizontal :global(.photo-card-media) {
      order: 2;
      flex: 1 1 0;
      min-width: 0;
      width: auto;
      height: auto;
      align-self: stretch;
      margin-block-end: 0;
    }
  }
</style>
