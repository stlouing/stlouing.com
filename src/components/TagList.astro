---
import { href } from '../lib/url'

interface Props {
  tags: string[]
}

const { tags } = Astro.props
---

{
  tags.length > 0 && (
    <p class="tags">
      {tags.map((tag) => (
        <a class="tag" href={href(`/tags/${tag}`)}>
          #{tag}
        </a>
      ))}
    </p>
  )
}
